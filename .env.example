# Shared password for /admin (defaults to fieldworx-admin if unset)
ADMIN_PASSWORD=fieldworx-admin

# Optional: override the value mixed into the admin session cookie
ADMIN_SESSION_SECRET=replace-me-in-production

# Optional: separate secret for customer login sessions
CUSTOMER_SESSION_SECRET=replace-me-in-production
